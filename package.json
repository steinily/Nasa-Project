{
  "name": "nasa-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "npm run watch --prefix server",
    "client": "npm start --prefix client",
    "watch": "npm-run-all --parallel server client ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " npm run build  --prefix client",
    "buildserver": "npm start --prefix server",
    "deploy": " npm-run-all build buildserver"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
